<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CPK_Calculate.ImportDataPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CPK_Calculate"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="16" Padding="20"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1" CornerRadius="12">
            <Grid ColumnSpacing="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Spacing="4">
                    <TextBlock Text="Excel Import" Style="{ThemeResource SubtitleTextBlockStyle}" FontWeight="SemiBold"/>
                    <TextBlock Text="เลือกไฟล์ Excel แล้วกำหนดข้อมูล CPK ก่อนนำเข้าฐานข้อมูล" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE14C;"/>
                        <TextBlock x:Name="RowCountText" Text="0 rows"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE160;"/>
                        <TextBlock x:Name="FileInfoText" Text="No file selected"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="16" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Padding="20" CornerRadius="12"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Spacing="16">
                        <Button x:Name="PickFileButton" Style="{StaticResource AccentButtonStyle}" Click="PickFileButton_Click">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE159;"/>
                                <TextBlock Text="เลือกไฟล์ Excel"/>
                            </StackPanel>
                        </Button>
                        <NumberBox x:Name="LslBox" Header="LSL" PlaceholderText="Lower Spec Limit" SpinButtonPlacementMode="Compact"/>
                        <NumberBox x:Name="UslBox" Header="USL" PlaceholderText="Upper Spec Limit" SpinButtonPlacementMode="Compact"/>
                        <NumberBox x:Name="SubgroupBox" Header="Subgroup Size" Value="5" Minimum="1" SpinButtonPlacementMode="Compact"/>
                        <TextBox x:Name="TitleBox" Header="Analysis Title" PlaceholderText="เช่น USS23 Feb 2026"/>
                        <TextBox x:Name="RecordedByBox" Header="Recorded By" PlaceholderText="ชื่อผู้บันทึก"/>
                        <TextBox x:Name="NoteBox" Header="Note" AcceptsReturn="True" TextWrapping="Wrap" Height="96" PlaceholderText="รายละเอียดเพิ่มเติม"/>
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <Button x:Name="ImportButton" Content="Import to Database" Style="{StaticResource AccentButtonStyle}" Click="ImportButton_Click" Width="170"/>
                            <Button Content="Clear" Click="ClearButton_Click" Width="100"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <Border Grid.Column="1" Padding="0" CornerRadius="12"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Padding="16" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" CornerRadius="12,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="80"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="#" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="Ratio" FontWeight="SemiBold"/>
                        <TextBlock Grid.Column="2" Text="Prod. Date" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                        <TextBlock Grid.Column="3" Text="Lot" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                        <TextBlock Grid.Column="4" Text="Value" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                    </Grid>
                    <ListView x:Name="PreviewListView" Grid.Row="1"
                              SelectionMode="None" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="0"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="local:ImportRow">
                                <Grid Padding="16,10" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,0.5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="1.5*"/>
                                        <ColumnDefinition Width="1*"/>
                                        <ColumnDefinition Width="80"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{x:Bind Index}" HorizontalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="{x:Bind Ratio}"/>
                                    <TextBlock Grid.Column="2" Text="{x:Bind ProductionDateText}" HorizontalAlignment="Center"/>
                                    <TextBlock Grid.Column="3" Text="{x:Bind LotNo}" HorizontalAlignment="Center"/>
                                    <TextBlock Grid.Column="4" Text="{x:Bind Value}" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border>
        </Grid>

        <InfoBar x:Name="StatusInfoBar" Grid.Row="2" Margin="16" IsOpen="False"/>

        <Grid x:Name="BusyOverlay" Background="#80000000" Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12">
                <ProgressRing Width="48" Height="48" IsActive="True"/>
                <TextBlock Text="Processing..." FontWeight="SemiBold" Foreground="White"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
