<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CPK_Calculate.RecordCPK"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CPK_Calculate"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RowDefinitions="Auto, Auto, *">

        <!-- Configuration Card -->
        <Border Grid.Row="0" Margin="24,24,24,8"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8" Padding="20" BorderThickness="1"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
            <StackPanel Spacing="16">
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <FontIcon Glyph="&#xE70F;" FontSize="24" Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                    <TextBlock Text="Record Configuration" FontSize="20" FontWeight="Bold"/>
                </StackPanel>

                <Grid ColumnDefinitions="*,*,Auto,Auto" ColumnSpacing="16">
                    <CalendarDatePicker x:Name="DateInput" Header="Date" HorizontalAlignment="Stretch"
                                        DateFormat="{}{day.integer}-{month.abbreviated}-{year.full}"/>

                    <ComboBox x:Name="GradeInput" Grid.Column="1" Header="Grade"
                              PlaceholderText="Select Product Grade" HorizontalAlignment="Stretch">
                        <ComboBoxItem Content="P0263"/>
                        <ComboBoxItem Content="P0251"/>
                        <ComboBoxItem Content="H0276"/>
                        <ComboBoxItem Content="P0241"/>
                    </ComboBox>

                    <Button Grid.Column="2" x:Name="GenerateBtn" Style="{StaticResource AccentButtonStyle}"
                            Padding="24,0" Height="32" VerticalAlignment="Bottom" Click="GenerateBtn_Click">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE710;" FontSize="14"/>
                            <TextBlock Text="Generate New Sheet"/>
                        </StackPanel>
                    </Button>
                    <Button Grid.Column="3" x:Name="ResetButton" Content="Reset Fields"
                            Padding="16,0" Height="32" VerticalAlignment="Bottom" Click="ResetButton_Click"/>
                </Grid>
            </StackPanel>
        </Border>

        <InfoBar x:Name="StatusInfoBar" Grid.Row="1" IsOpen="False" Margin="24,0,24,8"/>

    </Grid>
</Page>