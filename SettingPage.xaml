<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CPK_Calculate.SettingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CPK_Calculate"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="24,56,24,24">

        <StackPanel Spacing="24" Width="900" HorizontalAlignment="Center">

            <TextBlock Text="Settings" Style="{ThemeResource TitleLargeTextBlockStyle}" Margin="4,0,0,0" />

            <StackPanel Spacing="8">
                <TextBlock Text="Appearance &amp; behavior" Style="{ThemeResource BodyStrongTextBlockStyle}" Margin="4,0,0,0" />

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                        BorderThickness="1" 
                        CornerRadius="8">
                    <StackPanel>

                        <Grid Padding="16,12" MinHeight="68">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <FontIcon Glyph="&#xE790;" Margin="0,0,16,0" VerticalAlignment="Center" />

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="App theme" Style="{ThemeResource BodyTextBlockStyle}" />
                                <TextBlock Text="Select which app theme to display" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </StackPanel>

                            <ComboBox x:Name="ThemeComboBox" Grid.Column="2" Width="180" VerticalAlignment="Center" SelectionChanged="ThemeComboBox_SelectionChanged">
                                <ComboBoxItem Content="Light" Tag="Light" />
                                <ComboBoxItem Content="Dark" Tag="Dark" />
                                <ComboBoxItem Content="Use system setting" Tag="Default" IsSelected="True" />
                            </ComboBox>
                        </Grid>

                        <MenuFlyoutSeparator Margin="0"/>

                        <Grid Padding="16,12" MinHeight="68">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <FontIcon Glyph="&#xE1C5;" Margin="0,0,16,0" VerticalAlignment="Center" />

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Navigation style" Style="{ThemeResource BodyTextBlockStyle}" />
                                <TextBlock Text="Change the position of the navigation menu" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </StackPanel>

                            <ComboBox x:Name="NavComboBox" Grid.Column="2" Width="180" VerticalAlignment="Center" SelectionChanged="NavComboBox_SelectionChanged">
                                <ComboBoxItem Content="Left" Tag="Left" IsSelected="True" />
                                <ComboBoxItem Content="Top" Tag="Top" />
                            </ComboBox>
                        </Grid>

                    </StackPanel>
                </Border>
            </StackPanel>

            <StackPanel Spacing="8" Margin="0,8,0,0">
                <TextBlock Text="About" Style="{ThemeResource BodyStrongTextBlockStyle}" Margin="4,0,0,0" />

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                        BorderThickness="1" 
                        CornerRadius="8">
                    <Grid Padding="16,12" MinHeight="68">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Grid Width="32" Height="32" CornerRadius="6" Background="{ThemeResource SystemAccentColor}" Margin="0,0,16,0">
                            <FontIcon Glyph="&#xE814;" FontSize="16" Foreground="White" />
                        </Grid>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="CPK Calculation System" Style="{ThemeResource BodyTextBlockStyle}" FontWeight="SemiBold" />
                            <TextBlock Text="© 2026 Developer. All rights reserved." FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>

                        <TextBlock Grid.Column="2" Text="Version 1.0.0" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </Grid>
                </Border>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</Page>