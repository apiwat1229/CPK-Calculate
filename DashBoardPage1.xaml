<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CPK_Calculate.DashBoardPage1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CPK_Calculate"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="24,24,24,24">
        <StackPanel Spacing="28">

            <TextBlock Text="System Overview" Style="{ThemeResource TitleTextBlockStyle}" FontWeight="Bold" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,12,0">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <SymbolIcon Symbol="Target" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                            <TextBlock Text="Total Calculations" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <TextBlock Text="1,284" Style="{ThemeResource HeaderTextBlockStyle}" Margin="0,12,0,0" FontWeight="Bold"/>
                        <TextBlock Text="↑ 12% from last week" FontSize="12" Foreground="{ThemeResource SystemFillColorSuccessBrush}" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="20" Margin="6,0,6,0">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <SymbolIcon Symbol="Favorite" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                            <TextBlock Text="Avg. CPK (Monthly)" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <TextBlock Text="1.45" Style="{ThemeResource HeaderTextBlockStyle}" Margin="0,12,0,0" FontWeight="Bold"/>
                        <TextBlock Text="Status: Excellent" FontSize="12" Foreground="{ThemeResource SystemFillColorSuccessBrush}" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="20" Margin="12,0,0,0">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <SymbolIcon Symbol="OpenFile" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                            <TextBlock Text="Pending Exports" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <TextBlock Text="5" Style="{ThemeResource HeaderTextBlockStyle}" Margin="0,12,0,0" FontWeight="Bold"/>
                        <TextBlock Text="New reports ready" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,24,0" Spacing="24">
                    <StackPanel Spacing="12">
                        <TextBlock Text="CPK Performance Trend" Style="{ThemeResource SubtitleTextBlockStyle}" FontWeight="SemiBold"/>
                        <Border Height="250" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                            <Grid>
                                <TextBlock Text="[ CPK Trend Chart Placeholder ]" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorDisabledBrush}" />
                            </Grid>
                        </Border>
                    </StackPanel>

                    <StackPanel Spacing="12">
                        <TextBlock Text="Recent Calculations" Style="{ThemeResource SubtitleTextBlockStyle}" FontWeight="SemiBold"/>
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                            <ListView x:Name="RecentHistoryList" SelectionMode="None">
                                <ListView.Header>
                                    <Grid Padding="12,8" Background="{ThemeResource SubtleFillColorSecondaryBrush}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Part Name" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontWeight="Bold"/>
                                        <TextBlock Grid.Column="1" Text="Timestamp" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontWeight="Bold"/>
                                        <TextBlock Grid.Column="2" Text="Result" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontWeight="Bold" Margin="0,0,10,0"/>
                                    </Grid>
                                </ListView.Header>
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="local:HistoryItem">
                                        <Grid Padding="12,12">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{x:Bind PartName}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="1" Text="{x:Bind Date}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                                            <Border Grid.Column="2" Background="{ThemeResource SystemFillColorSuccessBackgroundBrush}" Padding="10,4" CornerRadius="12">
                                                <TextBlock Text="{x:Bind Result}" Foreground="{ThemeResource SystemFillColorSuccessBrush}" FontSize="12" FontWeight="Bold"/>
                                            </Border>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Border>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Spacing="12">
                    <TextBlock Text="Quick Actions" Style="{ThemeResource SubtitleTextBlockStyle}" FontWeight="SemiBold"/>
                    <Button HorizontalAlignment="Stretch" Height="60" Style="{ThemeResource AccentButtonStyle}">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <SymbolIcon Symbol="Add"/>
                            <TextBlock Text="New Calculation" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button HorizontalAlignment="Stretch" Height="60">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <SymbolIcon Symbol="Download"/>
                            <TextBlock Text="Download Template" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button HorizontalAlignment="Stretch" Height="60">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <SymbolIcon Symbol="Setting"/>
                            <TextBlock Text="Configure Limits" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Border Margin="0,12,0,0" Padding="16" Background="{ThemeResource SystemFillColorAttentionBackgroundBrush}" CornerRadius="8" BorderThickness="1" BorderBrush="{ThemeResource SystemFillColorAttentionBrush}">
                        <StackPanel Spacing="8">
                            <TextBlock Text="System Notification" FontWeight="Bold" Foreground="{ThemeResource SystemFillColorAttentionBrush}"/>
                            <TextBlock Text="Calibration for Gauge-A is due in 3 days. Please check hardware settings." FontSize="12" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</Page>